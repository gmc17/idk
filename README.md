Little genetic algorithm demonstration which allows the user to tune parameters, such as mutation rate.

Each species has DNA which takes the form of an array, theta[], of angles. At each timestep, a small force is applied to the specimen in the theta[t] direction.

Try this out on 
